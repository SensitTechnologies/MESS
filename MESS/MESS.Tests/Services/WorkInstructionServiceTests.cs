using System.Reflection;
using MESS.Data.Context;
using MESS.Services.WorkInstruction;

namespace MESS.Tests.Services.ProductionLog;

public class WorkInstructionServiceTests
{
    [Fact]
    public void ImportWorkInstruction_ValidFile_CorrectlyPrintsRichText()
    {
        // var service = new WorkInstructionService(new ApplicationContext());
        //
        // var codeBaseUrl = new Uri(Assembly.GetExecutingAssembly().Location);
        // var codeBasePath = Uri.UnescapeDataString(codeBaseUrl.AbsolutePath);
        // var dirPath = Path.GetDirectoryName(codeBasePath);
        // if (dirPath != null)
        // {
        //     var filePath = Path.Combine("./Resources/", "MESSRTFTest.xlsx");
        //     service.ImportFromXlsx(filePath);
        // }
        }
    }